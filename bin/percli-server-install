#!/usr/bin/env node
const { getDistributionArtifactsList } = require('../lib/DistributionHelper')

async function fetchArtifacts() {
  const packages = await getDistributionArtifactsList()
  console.log(packages)
  console.log('Implementation in progress')
}

fetchArtifacts()